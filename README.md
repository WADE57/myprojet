# myprojet

Courte description du projet (1-2 phrases).

## Objectif

Expliquer le but principal du projet.

## Contenu du depot

- `cprep.c` : [a completer]
- `wade.c` : [a completer]
- `projet.tex` : [a completer]

## Prerequis

- GCC (ou Clang)
- Linux/macOS/WSL (recommande)

## Compilation

```bash
mkdir -p build
gcc -std=c11 -Wall -Wextra -pedantic cprep.c -o build/cprep
gcc -std=c11 -Wall -Wextra -pedantic wade.c -o build/wade
```

## Usage

```bash
# Pour cprep 
./build/cprep [arguments]
# Pour wade
./build/wade [arguments]
```